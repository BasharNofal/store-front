import AppBar from '@material-ui/core/AppBar';
import Toolbar from '@material-ui/core/Toolbar'

const Header = (props) => {
    return (
        <AppBar><Toolbar style={{fontSize:"larger"}}>Our Store</Toolbar></AppBar>
    )
}

export default Header